{
  "name": "date-holidays-ical",
  "version": "0.1.1",
  "description": "Generate ical files from date-holidays data",
  "main": "lib",
  "bin": {
    "holidays-ical": "bin/holidays-ical.js"
  },
  "scripts": {
    "test": "mocha test/*.mocha.js",
    "lint": "eslint --quiet --ignore-path .gitignore *.js **/*.js",
    "pack": "npm run lint && npm test && npm pack"
  },
  "author": "commenthol <commenthol@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.9.0",
    "date-holidays": "^0.1.5",
    "resig": "^1.0.1"
  },
  "devDependencies": {
    "eslint": "^2.8.0",
    "eslint-config-standard": "^5.1.0",
    "eslint-plugin-promise": "^1.1.0",
    "eslint-plugin-standard": "^1.3.2",
    "mocha": "^2.4.5"
  },
  "eslintConfig": {
    "extends": "standard",
    "plugins": [
      "standard"
    ],
    "rules": {
      "no-console": 2
    }
  }
}
